<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- CRÍTICO: sin esto, `php artisan test` usa la base de datos de desarrollo
             del .env (RefreshDatabase la migra en limpio y la deja vacía). SQLite en
             memoria no sirve aquí porque las migraciones usan sintaxis de MySQL
             (p. ej. ALTER TABLE ... MODIFY). Se usa una base de datos de pruebas
             separada, con el mismo nombre que ya usa CI (.github/workflows/ci.yml).
             force="true" para que esto valga siempre, incluso si el shell ya trae
             DB_DATABASE definida (como en CI, donde además coincide el valor). -->
        <env name="DB_DATABASE" value="convalidaciones_test" force="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
